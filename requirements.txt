# Production v8 best-pipeline dependencies.
#
# Targets Python 3.11+ on macOS (mps), CUDA 12.x on NVIDIA Hopper, or
# CPU. torch / torchvision are NOT pinned here -- install the platform-
# specific build first, e.g.:
#   pip install torch torchvision \
#       --index-url https://download.pytorch.org/whl/cu124
# then `pip install -r requirements.txt`.

# Detection + tracking
ultralytics>=8.4.37,<9
boxmot>=10.0.52
lap>=0.5.12

# Numerics / vision
numpy>=1.26,<3
scipy>=1.11
opencv-python-headless>=4.9,<4.11
imageio>=2.34
imageio-ffmpeg>=0.5
ffmpeg-python>=0.2.0

# Pickling (cache + tracks.pkl)
joblib>=1.3
