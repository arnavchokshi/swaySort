# Production Stage 1 ID/tracking dependencies.
#
# Targets Python 3.11 on macOS (mps), CUDA 12.x on NVIDIA, or CPU.
# Versions are pinned for reproducible tracker/evaluator behavior. Torch and
# torchvision stay out of this file because the right wheel depends on your
# platform.
#
# torch / torchvision are NOT pinned in this file because the right
# wheel depends on your platform (CUDA vs MPS vs CPU). Install them
# FIRST, then `pip install -r requirements.txt`. See README "Install".

# Detection + tracking
ultralytics==8.4.37
boxmot==18.0.0
lap==0.5.13
lapx==0.9.4
filterpy==1.4.5

# Numerics / vision
numpy==1.26.4
scipy>=1.13,<1.14
opencv-python==4.10.0.84

# Pickling (cache + tracks.pkl)
joblib==1.5.3

# Required by ultralytics + boxmot at runtime; pinned for reproducibility
tqdm==4.67.3

# Only needed to regenerate the historical comparison charts via
# scripts/generate_comparison_charts.py. Safe to remove if you only
# want to run the production pipeline.
matplotlib==3.10.8

# Only needed to score IDF1/MOTA vs CVAT/MOT ground truth via
# scripts/eval_id_yolo_cvat.py.
# Safe to remove if you only want to run the production pipeline.
motmetrics==1.4.0
